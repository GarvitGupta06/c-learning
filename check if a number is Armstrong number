/*Program to check if a given 3 digit number is
Armstrong number or not.
Garvit Gupta
July 2025*/
#include <stdio.h>
#include<math.h>
int main(){
    int num,a1,a2,a3,newnum;
    printf("Enter a three digit number:");
    scanf("%d",&num);
    a1=num%10;
    newnum=num/10;
    a2=newnum%10;
    newnum/=10;
    a3=newnum%10;
    if(pow(a1,3)+pow(a2,3)+pow(a3,3)==num){
        printf("It is an armstrong number");
    } else {
        printf("It is not an armstrong number");
    }
    return 0;
}
